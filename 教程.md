# Vanction 编程语言教程

欢迎来到Vanction编程语言教程！本教程将从基础开始，逐步介绍Vanction语言的各种特性和用法。

## 1. 环境搭建

### 1.1 安装必要工具

在开始之前，你需要安装以下工具：

- **CMake**：用于构建项目
- **MinGW-w64 GCC**：C++编译器

### 1.2 构建Vanction

按照README.md中的安装步骤构建Vanction编译器：

```bash
git clone https://github.com/bilibili-dideng/VanctionProgrammingLanguage.git
cd VanctionProgramLanguage
mkdir build
cd build
cmake ..
cmake --build .
```

### 1.3 验证安装

运行Hello World示例来验证安装：

```bash
cd ..
./build/Vanction examples/test/hello_world.vn
```

如果看到输出"Hello, World!"，说明安装成功。

## 2. 基础语法

### 2.1 Hello World

让我们从经典的Hello World程序开始：

```vanction
func main() {
    std:io.print("Hello, Vanction!");
    return 0;
}
```

- `func`关键字用于定义函数
- `main()`是程序的入口函数
- `std:io.print()`用于输出内容，默认不添加换行符
- `return`语句用于返回值

### 2.2 用户输入
使用`std:io.input()`函数获取用户输入：

```vanction
func main() {
    std:io.print("请输入你的姓名：");
    var name = std:io.input();
    std:io.print("你好，" + name + "!");
    return 0;
}
```

### 2.3 注释

Vanction支持单行注释和多行注释：

```vanction
func main() {
    || 这是一个单行注释
    var a = 10; || 行尾注释
    |* 这是一个
   多行注释 *|
   |\ 这是一个文档注释
   * 用于函数或模块的文档说明
   /|
   """ 这也是一个
   多行注释 """
}

## 3. 变量和类型

### 3.1 变量声明

使用`var`关键字声明变量：

```vanction
var x = 10;          || 整数
var name = "Vanction";  || 字符串
var is_active = true;   || 布尔值
var pi = 3.14159;       || 浮点数
```

### 3.2 常量声明

使用`immut var`声明不可变常量：

```vanction
immut var PI = 3.14159;
immut var VERSION = "1.0.0";
```

常量一旦声明，就不能被修改，否则会引发`ImmutError`。

### 3.3 类型系统

Vanction是动态类型语言，变量类型会根据赋值自动推断：

- **整数**：`10`, `-5`, `0`
- **浮点数**：`3.14`, `-0.5`, `1.0`
- **字符串**：`"hello"`, `'world'`
- **布尔值**：`true`, `false`
- **列表**：`[1, 2, 3]`
- **哈希表**：`{"key": "value"}`
- **对象**：`instance Person("Alice")`

## 4. 函数

### 4.1 函数定义

使用`func`关键字定义函数：

```vanction
func add(a, b) {
    return a + b;
}
```

### 4.2 函数调用

```vanction
func main() {
    var result = add(5, 3);
    std:io.print("5 + 3 = " + result);
    return 0;
}
```

### 4.3 函数参数

函数可以接受任意数量的参数，参数类型会自动推断：

```vanction
func greet(name, age) {
    std:io.print("Hello, " + name + "! You are " + age + " years old.");
}
```

### 4.4 返回值

使用`return`语句返回值：

```vanction
func multiply(a, b) {
    return a * b;
}
```

如果没有返回值，函数默认返回`undefined`。

## 5. 控制流

### 5.1 条件语句

#### 5.1.1 if语句

```vanction
func main() {
    var x = 10;
    
    if (x > 5) {
        std:io.print("x is greater than 5");
    } else-if (x < 5) {
        std:io.print("x is less than 5");
    } else {
        std:io.print("x is equal to 5");
    }
    
    return 0;
}
```

#### 5.1.2 逻辑运算符

- `&`：逻辑与
- `|`：逻辑或
- `^`：逻辑异或

```vanction
if (x > 5 & y < 10) {
    || 执行代码
}
```

### 5.3 switch语句

Vanction支持switch语句，用于基于不同条件值执行不同的代码块：

```vanction
func main() {
    var num = 2;
    
    switch (num) {
        case 1 {
            std:io.print("Case 1 executed");
        }
        case 2 {
            std:io.print("Case 2 executed");
        }
        case 3 {
            std:io.print("Case 3 executed");
        }
    }
    
    return 0;
}
```

**说明**：
- `switch`关键字后面跟着要测试的表达式
- `case`关键字后面跟着要匹配的值
- 每个case块用`{}`包围
- 当匹配到某个case时，执行该case的代码块
- 默认情况下，不会自动break，会继续执行下一个case（fallthrough行为）

**支持的类型**：
- 整数
- 字符串
- 布尔值
- 浮点数/双精度数

```vanction
|| 字符串switch
var str = "hello";
switch (str) {
    case "hello" {
        std:io.print("Hello case executed");
    }
    case "world" {
        std:io.print("World case executed");
    }
}

|| 布尔值switch
var flag = true;
switch (flag) {
    case true {
        std:io.print("True case executed");
    }
    case false {
        std:io.print("False case executed");
    }
}
```

### 5.4 循环结构

#### 5.4.1 while循环

```vanction
func main() {
    var i = 0;
    while (i < 5) {
        std:io.print("i = " + i);
        i = i + 1;
    }
    return 0;
}
```

#### 5.4.2 do-while循环

```vanction
func main() {
    var i = 0;
    do {
        std:io.print("i = " + i);
        i = i + 1;
    } while (i < 5);
    return 0;
}
```

#### 5.4.3 for循环

```vanction
func main() {
    for (var i = 0; i < 5; i = i + 1) {
        std:io.print("i = " + i);
    }
    return 0;
}
```

#### 5.4.4 for-in循环

用于遍历集合：

```vanction
func main() {
    || 遍历列表
    var numbers = [1, 2, 3, 4, 5];
    for (var num in numbers) {
        std:io.print("Number: " + num);
    }
    
    || 遍历哈希表
    var person = {"name": "Bob", "age": 25};
    for (var key, var value in person) {
        std:io.print(key + ": " + value);
    }
    
    || 遍历范围
    for (var i in range(5)) {
        std:io.print("i = " + i);
    }
    
    return 0;
}
```

## 6. 类和对象

### 6.1 类定义

使用`class`关键字定义类：

```vanction
class Person() {
    || 成员变量
    var name;
    var age;
    
    || 构造函数
    instance.init(instance, name, age) {
        instance.name = name;
        instance.age = age;
    }
    
    || 成员方法
    instance.greet(instance) {
        std:io.print("Hello, my name is " + instance.name);
    }
    
    instance.get_age(instance) {
        return instance.age;
    }
}
```

### 6.2 创建对象

使用`instance`关键字创建对象：

```vanction
func main() {
    var person = instance Person("Alice", 30);
    person.greet();
    std:io.print("Age: " + person.get_age());
    return 0;
}
```

### 6.3 继承

支持单继承：

```vanction
class Employee(Person) {
    var job_title;
    
    instance.init(instance, name, age, job_title) {
        || 调用父类构造函数
        Person.init(name, age);
        this.job_title = job_title;
    }
    
    instance.work() {
        std:io.print(this.name + " is working as a " + this.job_title);
    }
}

func main() {
    var emp = instance Employee("Bob", 25, "Software Engineer");
    emp.greet();  || 继承自Person类
    emp.work();   || Employee类自己的方法
    return 0;
}
```

## 7. 错误处理

Vanction使用`try-happen`机制处理错误：

```vanction
func main() {
    try {
        var result = 10 / 0;  || 这会引发DivideByZeroError
        std:io.print("Result: " + result);
    } happen (DivideByZeroError) as e {
        std:io.print("Caught an error:");
        std:io.print("  Text: " + e.text);
        std:io.print("  Type: " + e.type);
        std:io.print("  Info: " + e.info);
    }
    
    return 0;
}
```

### 7.1 内置错误类型

- `Error`：所有错误的基类
- `DivideByZeroError`：除零错误
- `ValueError`：值错误
- `TypeError`：类型错误
- `MethodError`：方法错误
- `ImmutError`：常量修改错误
- `RangeError`：范围错误
- `ListIndexError`：列表索引错误

### 7.2 捕获所有错误

使用`Error`类型可以捕获所有错误：

```vanction
try {
    || 可能引发错误的代码
} happen (Error) as e {
    std:io.print("Caught an error: " + e.text);
}
```

## 8. 数据结构

### 8.1 列表（List）

列表是有序的元素集合：

```vanction
func main() {
    || 创建列表
    var numbers = [1, 2, 3, 4, 5];
    
    || 访问元素
    std:io.print("First element: " + numbers[0]);
    std:io.print("Last element: " + numbers[-1]);  || 支持负索引
    
    || 修改元素
    numbers[2] = 10;
    
    || 添加元素
    numbers.add(6);
    
    || 获取列表长度
    std:io.print("Length: " + numbers.size());
    
    || 遍历列表
    for (var num in numbers) {
        std:io.print(num);
    }
    
    return 0;
}
```

### 8.2 哈希表（HashMap）

哈希表是键值对集合：

```vanction
func main() {
    || 创建哈希表
    var person = {
        "name": "Alice",
        "age": 30,
        "city": "New York"
    };
    
    || 访问值
    std:io.print("Name: " + person["name"]);
    
    || 修改值
    person["age"] = 31;
    
    || 添加新的键值对
    person["email"] = "alice@example.com";
    
    || 遍历哈希表
    for (key, value in person) {
        std:io.print(key + ": " + value);
    }
    
    || 获取所有键
    var keys = person.key();
    std:io.print("Keys: " + keys);
    
    return 0;
}
```

## 9. 模块和导入

### 9.1 模块系统

Vanction支持将代码组织为模块，便于重用和管理：

```vanction
|| 导入模块
import math;

func main() {
    var result = math.add(5, 3);
    std:io.print("Result: " + result);
    return 0;
}
```

### 9.2 增强的导入语法

Vanction支持更灵活的导入语法：

#### 9.2.1 导入指定文件夹中的文件

```vanction
|| 导入xxx文件夹中的xx文件
import xxx.xx;

func main() {
    || 使用导入的模块功能
    return 0;
}
```

#### 9.2.2 智能模块导入

```vanction
|| 智能导入：首先检查当前目录下是否有xxx.vn文件
|| 如果没有，查找是否有xxx文件夹后导入文件夹中的_package_.vn文件
|| 如果没有此文件则报错
import xxx;

func main() {
    || 使用导入的模块功能
    return 0;
}
```

#### 9.2.3 导入时指定别名

```vanction
|| 使用别名导入模块
import math.utils to utils;

func main() {
    var result = utils.add(5, 3);
    std:io.print("Result: " + result);
    return 0;
}
```

#### 9.2.4 选择性导入成员

```vanction
|| 选择性导入模块中的特定成员
import math using add, subtract;

func main() {
    var sum = add(5, 3);
    var diff = subtract(5, 3);
    std:io.print("Sum: " + sum + ", Difference: " + diff);
    return 0;
}
```

#### 9.2.5 结合使用别名和选择性导入

```vanction
|| 结合使用别名和选择性导入
import math.utils to utils using add, multiply;

func main() {
    var sum = utils.add(5, 3);
    var product = utils.multiply(5, 3);
    std:io.print("Sum: " + sum + ", Product: " + product);
    return 0;
}
```

### 9.3 C++模块导入 (cimport)

Vanction支持直接导入C++模块，实现与C++代码的无缝集成：

#### 9.3.1 基本用法

```vanction
|| 导入C++模块
cimport test;

func main() {
    || 调用C++函数
test.hello();
    
    || 使用C++函数返回值
    var sum = test.add(3, 4);
    std:io.print("Sum: " + sum);
    
    return 0;
}
```

#### 9.3.2 导入指定文件夹中的C++文件

```vanction
|| 导入xxx文件夹中的xx C++文件
cimport xxx.xx;

func main() {
    || 使用导入的C++模块功能
    return 0;
}
```

#### 9.3.3 选择性导入C++模块成员

```vanction
|| 选择性导入C++模块中的特定成员
cimport graphics using drawCircle, drawRectangle;

func main() {
    || 直接使用导入的函数
drawCircle(100, 100, 50);
drawRectangle(200, 200, 100, 150);
    return 0;
}
```

#### 9.3.4 使用别名导入C++模块

```vanction
|| 使用别名导入C++模块
cimport graphics to g;

func main() {
    || 通过别名使用模块功能
g.drawCircle(100, 100, 50);
    return 0;
}
```

#### 9.3.5 结合使用别名和选择性导入

```vanction
|| 结合使用别名和选择性导入
cimport graphics to g using drawCircle;

func main() {
    || 通过别名使用选择性导入的函数
g.drawCircle(100, 100, 50);
    return 0;
}
```

### 9.4 命名空间

使用命名空间组织代码：

```vanction
|| 定义命名空间
namespace utils {
    func add(a, b) {
        return a + b;
    }
    
    func subtract(a, b) {
        return a - b;
    }
}

func main() {
    var sum = utils:add(5, 3);
    var diff = utils:subtract(5, 3);
    std:io.print("Sum: " + sum + ", Diff: " + diff);
    return 0;
}
```

## 10. 字符串操作

### 10.1 字符串连接

使用`+`运算符连接字符串：

```vanction
var name = "Vanction";
var greeting = "Hello, " + name + "!";
std:io.print(greeting);  || 输出: Hello, Vanction!
```

### 10.2 字符串索引

访问字符串中的单个字符：

```vanction
var str = "Hello";
std:io.print(str[0]);  || 输出: H
std:io.print(str[-1]); || 输出: o
```

### 10.3 字符串方法

```vanction
var str = "Hello, Vanction!";
std:io.print(str.length());  || 获取长度
std:io.print(str.upper());   || 转换为大写
std:io.print(str.lower());   || 转换为小写
std:io.print(str.contains("Vanction"));  || 检查是否包含子字符串
```

## 11. 高级特性

### 11.1 闭包

Vanction支持闭包，闭包是指一个函数可以访问其词法作用域之外的变量，即使该函数在其词法作用域之外执行。

#### 11.1.1 使用func关键字实现闭包

```vanction
func make_counter() {
    var count = 0;
    
    func increment() {
        count = count + 1;
        return count;
    }
    
    return increment;
}

func main() {
    var counter = make_counter();
    std:io.print(counter());  || 输出: 1
    std:io.print(counter());  || 输出: 2
    std:io.print(counter());  || 输出: 3
    return 0;
}
```

#### 11.1.2 函数作为参数的闭包示例

```vanction
func test_closure(abc) {
    func map() {
        return abc;
    }
    return map;
}

func main() {
    var closure_func = test_closure(32);
    std:io.print(closure_func());  || 输出: 32
}
```

在这个例子中，`map`函数可以访问`closure__`函数的`abc`参数，即使`map`函数在`closure__`函数外部被调用。这就是闭包的核心特性 - 保留对外部变量的引用。

#### 11.1.3 闭包的特点

1. **数据封装**：闭包可以将数据和操作数据的函数绑定在一起
2. **状态保持**：闭包可以保持函数调用时的状态
3. **模块化**：闭包可以创建独立的函数模块
4. **延迟执行**：闭包可以延迟函数的执行

闭包在函数式编程中非常有用，可以用来创建高阶函数、实现回调机制等。

### 11.2 Lambda表达式

支持匿名函数：

```vanction
func main() {
    var add = lambda (a, b) -> a + b;
    std:io.print(add(5, 3));  || 输出: 8
    return 0;
}
```

### 11.3 类型转换

```vanction
var num_str = "123";
var num = std:type.int(num_str);  || 字符串转整数
std:io.print(num);  || 输出: 123

var float_str = "3.14";
var float_num = std:type.float(float_str);  || 字符串转浮点数
std:io.print(float_num);  || 输出: 3.14

var int_num = 42;
var str_num = std:type.string(int_num);  || 整数转字符串
std:io.print(str_num);  || 输出: "42"
```

## 12. 标准命名空间

### 12.1 命名空间介绍

Vanction提供了`std:`标准命名空间，用于存放所有原生函数和类，避免与用户自定义标识符发生命名冲突。

### 12.2 常用标准函数

#### 12.2.1 I/O函数

- `std:io.print(content)`：输出指定内容，默认不添加换行符
- `std:io.input()`：无提示文本，直接等待用户输入
- `std:io.input(prompt)`：输出指定提示文本并等待用户输入

#### 12.2.2 类型转换函数

- `std:type.int(value)`：转换为整数
- `std:type.float(value)`：转换为浮点数
- `std:type.double(value)`：转换为双精度浮点数
- `std:type.char(value)`：转换为字符
- `std:type.string(value)`：转换为字符串

### 12.3 命名空间语法

命名空间使用`:`分隔符，例如：

```vanction
std:io.print("Hello, World!");
var input = std:io.input("Enter something: ");
var num = std:type.int(input);
```

## 13. 标准库系统

### 13.1 标准库结构

Vanction的标准库位于程序运行目录下的`stdlib`文件夹中，采用"文件夹+_package_.vn"的组织结构。每个库以文件夹形式存在，内部包含`_package_.vn`作为库入口文件。

### 13.2 标准库访问

```vanction
|| 导入标准库模块
import stdlib.math;

func main() {
    var result = stdlib.math.add(5, 3);
    std:io.print("Result: " + result);
    return 0;
}
```

## 14. 最佳实践

1. **命名规范**：使用下划线命名法（snake_case）
2. **代码风格**：保持一致的缩进和换行
3. **错误处理**：总是处理可能出现的错误
4. **模块化**：将代码组织为多个模块
5. **注释**：为复杂代码添加注释
6. **测试**：编写测试用例验证代码正确性

## 15. 示例程序

在`examples`目录下提供了多个示例程序，展示了Vanction语言的各种特性：

- `hello_world.vn`：简单的Hello World程序
- `basic_arithmetic.vn`：基本算术运算
- `conditionals_loops.vn`：条件语句和循环
- `switch_test.vn`：switch语句使用
- `test_error_handling.vn`：错误处理机制
- `test_data_structures.vn`：数据结构使用
- `hash_map.vn`：哈希表使用示例
- `namespace_class.vn`：命名空间和类

## 16. 常见问题

### 16.1 为什么我的程序编译失败？

检查以下几点：
- 语法错误
- 缺少依赖
- 变量未声明
- 类型不匹配

### 16.2 如何调试程序？

使用`std:io.print()`输出调试信息，或者在编译时添加调试标志。

### 16.3 如何扩展Vanction？

可以通过修改源代码来扩展Vanction语言，主要涉及以下文件：
- `lexer.cpp`：词法分析器
- `parser.cpp`：语法分析器
- `code_generator.cpp`：代码生成器

## 17. 进一步学习

- 阅读`开发文档.md`了解Vanction的内部实现
- 查看示例程序学习实际用法
- 参与项目开发，贡献代码

---

恭喜你完成了Vanction编程语言教程！现在你已经掌握了Vanction的基本特性和用法，可以开始编写自己的程序了。

祝你编程愉快！🚀